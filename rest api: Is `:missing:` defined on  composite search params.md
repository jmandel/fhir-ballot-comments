## Is `:missing:` defined on  composite search params?

The language in `6.11.2.1.1` should be updated to refer to composite search
parameters as "composite search parameters" rather than "combination".
